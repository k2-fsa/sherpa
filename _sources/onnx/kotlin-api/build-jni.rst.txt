.. _sherpa-onnx-jni-build-kotlin:

Build JNI interface
===================

Build the JNI interface with the standard `sherpa-onnx`_ CMake workflow on
macOS or Linux.

.. hint::

   For Windows users, you have to modify the commands by yourself.
